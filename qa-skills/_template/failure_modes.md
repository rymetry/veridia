# Failure Modes

- Input contract drift: the incoming artifact no longer matches `input.schema.json`.
- Unverifiable output: the skill cannot produce an output with sufficient source grounding.
- Validator gap: the package has a procedure but lacks a validator for a material quality condition.
